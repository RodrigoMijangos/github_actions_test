name: Example Workflow

on:
    push:
        branches:
            feature-0001-mijangos # Comment

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps: 
            - name: Decir hola
              run: echo "Hola Mundo"

            - name: Esperar
              run: sleep 10

            - name: Decir Adiós
              run: echo "Adiós"
